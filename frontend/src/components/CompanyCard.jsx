import React, { forwardRef } from 'react';

const CompanyCard = forwardRef((props, ref) => {
    const {

    } = props;

    return (
        <div>

        </div>
    )
});

export default CompanyCard;